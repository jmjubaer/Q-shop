import { Link } from "react-router-dom";

const Navbar = () => {
    return (
        <div>
            <Link to={"/"}>Q Shop</Link>
            <nav>
                <ul>
                    <li></li>
                </ul>
            </nav>
        </div>
    );
};

export default Navbar;
